import Application from './Application';

/**
 * The internal slave exposes the Application object as an entry point.
 * You can use it for tests if you require access the internals of the library
 */
export default {
    'Application': Application,
};
